import { Header } from "../components/Header";


// 프로필 화면에 로그인 성공 시
// 원래 사용하고자 하는 페이지로 리다이렉트하는 기능 추가

const Profile = () => {
  return (
    <div>
        <Header />
      <div>관심필기 조회 화면</div>
    </div>
  );
};
export default Profile;
